import { BulletedList } from "mila-components";

const code = `() => {
  return (
    <BulletedList styleType="number">
      <li>Get started</li>
      <li>Foundations</li>
      <li>Components</li>
    </BulletedList>
  );
}`;

const scope = { BulletedList };

export default { code, scope };
