import SpacingPage from "@/screens/foundations/spacing/spacing";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Spacing - Mila components",
  description: "Spacing foundations of the Mila component library.",
};

export default function Spacing() {
  return <SpacingPage />;
}
